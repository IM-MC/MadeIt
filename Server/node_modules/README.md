all the files were not uploaded, but using the command 'npm install' will download any packages that is specified in the packages.json 
file and it should store everything in the node_modules/ folder
